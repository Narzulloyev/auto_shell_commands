# replaces the word color with the background/
setterm -inversescreen on

# back to the old background use off
# setterm -inversescreen off

# background is a white and words is black
setterm -background white | setterm -foreground black

# other color background = black, red, green, yellow, blue, magenta, cyan, or white
# foreground = black, red, green, yellow, blue, magenta, cyan, or white
# back to the default use --reset
# setterm --reset

